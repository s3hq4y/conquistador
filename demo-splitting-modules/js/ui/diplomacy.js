const UIDiplomacy = {
    updateLauncher(game) {
        const dipBtn = document.getElementById('diplomacy-launcher');
        if (!dipBtn) return;
        dipBtn.classList.remove('hidden');
    }
};
